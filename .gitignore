/target/

# Sparse clone of MicrosoftDocs/query-docs — kept locally for the
# tools/extract_ms_docs.py pipeline; the parsed JSON in
# mrsflow/stdlib-reference/ is the committed artefact.
/tools/ms-docs-mirror/

# Local-only working artefacts (not shared)
/.claude/
/.vscode/
/.devlog/
/.corpus_tmp/
/.em_tmp/

# Large local example corpus — kept locally for triage, not shared
/examples/RIVSTS*/
